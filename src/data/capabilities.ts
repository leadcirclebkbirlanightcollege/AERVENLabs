import { Capability } from '../types';

/**
 * Technology capabilities and services data layer.
 * Empty typed array establishing the data schema without unverified capabilities.
 */
export const capabilities: Capability[] = [];
